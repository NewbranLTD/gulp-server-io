'use strict';
const assert = require('assert');
// Const gulpServerIo = require('../index.js');

describe('gulpServerIo', () => {
  it('has a test', () => {
    assert(false, 'gulpServerIo should have a test');
  });
});
